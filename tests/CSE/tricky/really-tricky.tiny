// PASSED
main
var i, x, y, j;
{
    let i <- call InputNum();
    let x <- 0;
    let y <- 0;
    let j <- i;

    while x < 10 do
        let x <- i + 1;
        let y <- j + 1;
        if x < 10 do
            let x <- y + 1;
        fi;
        let j <- 2;
    od;

	call OutputNum(i);
	call OutputNum(x);
	call OutputNum(y);
	call OutputNum(j);
}.
