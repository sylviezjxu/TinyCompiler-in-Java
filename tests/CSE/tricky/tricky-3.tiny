// PASSED
main
var i, x, y, j, k;
{
    let i <- call InputNum();
    let x <- 0;
    let y <- 0;
    let j <- i;
    let k <- i;

    while x < 10 do
        let x <- i + 1;
        let y <- j + 1;
        let k <- k + 1;
        let i <- j + 1;
    od;

	call OutputNum(i);
	call OutputNum(x);
	call OutputNum(y);
	call OutputNum(j);
}.
