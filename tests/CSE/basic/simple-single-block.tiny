// PASSED

main
var a, b;
{
    let a <- call InputNum();
    let b <- a;
    let c <- b;
    let d <- c;

    let a <- a + 1;
    let b <- b + 1;
    let c <- c + 1;
    let d <- d + 1;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
	call OutputNum(d);
}.