// PASSED
main
var a, b, c, d, i;
{
    let a <- 10;
    let i <- a;
    let b <- a + 1 * 2;
    let c <- i + 1 * 2;
    let d <- c + a

	call OutputNum(a);
	call OutputNum(b);
    call OutputNum(c);
    call OutputNum(d);
}.