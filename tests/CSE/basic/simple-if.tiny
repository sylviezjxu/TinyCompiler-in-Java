// PASSED

main
var a, b, c;
{
    let a <- call InputNum();
    let b <- a;
    let c <- a + 1;

    if a < b then
        let a <- a + 1;
    else
        let c <- b + 1;
    fi;
    let a <- 999 + c;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.