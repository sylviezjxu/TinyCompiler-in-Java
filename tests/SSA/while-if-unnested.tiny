main
var a, b, c, d;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    while a < c do
        let a <- a + 1;
    od;
    if a < d then
        call OutputNum(a);
    else
        let b <- a + 1;
    fi;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.
