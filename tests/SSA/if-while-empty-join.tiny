main
var a, b, c, d;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    if a < d then
        call OutputNum(a);
    else
        call OutputNum(d);
    fi;
    while a < c do
        let a <- a + 1;
    od;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.
