main
var a, b, c, d;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    while a < 100 do
        let c <- c + 1;
    od;
    let a <- a + 1;
    let d <- b + 1;


	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.