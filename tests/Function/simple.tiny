main
var a, b, c, d;

void function myFunc1(var1, var2);
{
    call OutputNum(100);
}.

function myFunc2();
var a, b;
{
    let a <- call InputNum();
    call OutputNum(a);
    return a;
}.

{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    if b < a then
        let b <- b + 1;
    else
        let b <- b + 3;
    fi;
    let a <- a + 1;


	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.