main
var a, b, c, d;

void function myFunc1(var1, var2);
{
    call OutputNum(100);
};

function myFunc2();
var a, b;
{
    let a <- call InputNum();
    call OutputNum(a + 1);
    return a;
};

{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();
    let d <- b;

    if b < a then
        let b <- b + 1;
    else
        let b <- b + 3;
    fi;
    let a <- a + 1;
    call myFunc1(1, 3);

	call OutputNum(a + b);
	call OutputNum(b);
	call OutputNum(c);
}.
