main
var a, b, c;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();

    if b < 0 then
        let b <- b + 1;
        while a < 100 do
            let a <- a + 2;
        od;
    else
        if c < 0 then
            let c <- c + 1;
        else
            let c <- c + 99;
        fi;
    fi;
    let a <- a + 1;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.
