main
var a, b, c;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();

    if c < 0 then
        let c <- c + 1;
    else
        let a <- a + 2;
        while a < 100 do
            let b <- b + 1;
        od;
    fi;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.
