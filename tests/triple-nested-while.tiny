main
var a, b, c;
{
    let a <- call InputNum();
    let b <- call InputNum();
    let c <- call InputNum();

	while a < 100 do
	    let a <- a + 9;
	    while b < 0 do
	        let b <- b + 8;
	        while c < 0 do
	            let c <- c + 999;
	        od;
	    od;
	od;

	call OutputNum(a);
	call OutputNum(b);
	call OutputNum(c);
}.
