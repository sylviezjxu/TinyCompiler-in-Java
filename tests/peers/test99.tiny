main var a,b,c,d,e,g,f; {
  let a <- 2;
  let b<-a+2;
  let c<-a+b;
  let d<-(a+b)*4+c;
  let f<-0;
  let e<-0;
  let g<-0;
  while a > 0 do
    if b > c+d then
      if c > 0 then
        let e<-20;
      else
        let f<-a+b;
      fi;
    else
      let g<-f+((a+b)*4);
    fi;
  od;
  let g<-e+a;
  }.
