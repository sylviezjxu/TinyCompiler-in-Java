main
var a, b, c, d, e, f, g, h;
{
    let a <- 1;
    let b <- a + 1;
    let c <- b + 1;
    let d <- c + 1;
    let e <- d + 1;
    let f <- e + 1;
    let g <- a + b + c + d + e + f;
    if f < 0 then
      let h <- a + b + c + d + e + f + 10 + 20 + 30;
    fi;

    call OutputNum(h);
}.
