main
var a, b, c;

{
    let a <- 1;
    let b <- 2;
    let c <- 1;
    while a < b do
        let a <- a + 1;
        while b != 0 do
            let c <- c + 1;
        od;
    od;

    call OutputNum(a);
    call OutputNum(c);
}.
