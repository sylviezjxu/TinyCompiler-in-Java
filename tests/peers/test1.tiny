main

var alpha,beta,gamma;
var v0, v1, v2;

function f1 ( arg0, arg1, arg2 );
    var fvar0;
    var fvar1;
    {
        if arg0 > 0
        then
            let fvar0 <- arg1 / 2 + arg2;
        else
            let fvar0 <- 0;
        fi;
        return fvar0;
    };

{
    let v0 <- 5;
    let v1 <- 1;
    while v0 > 0
    do
        let v0 <- v0 - 1;
        let v1 <- v1 * 2;
    od;
    call OutputNum(call f1(1,v1,1));
}.
